// methods of buffer module
/*const buff = new Buffer.alloc(5,"amanq");
console.log(buff);

const buff= new Buffer.alloc(4,"name");
const buff1= new Buffer.alloc(4,"name");
console.log(new Buffer.from(buff).equals(buff1));
*/
/*const buff= new Buffer.alloc(4,"name");
const buff1= new Buffer.alloc(4,"mesf");
console.log(buff1.toString());
buff.copy(buff1);
console.log(buff1.toString());
*/
/*
const buff= new Buffer.alloc(4,"name");
const buff1= new Buffer.alloc(4,"mesf");
console.log(buff1.length);
*/
/*
const myJson = new Buffeer.alloc(10,{name:'gfg',age:'gffd'});
console.log(myJson.toJSON());
*/